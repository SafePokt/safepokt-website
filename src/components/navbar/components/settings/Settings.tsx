import { IconButton } from 'components/iconButton'

// TODO: Add missing functionality.
export const Settings = () => {
  return <IconButton icon='settings' onClick={() => console.log('settings')} />
}
